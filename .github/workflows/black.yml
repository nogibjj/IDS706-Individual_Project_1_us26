name: Black Formatter

on:
  workflow_run:
    workflows: ["install"]
    branches: ["main"]
    types: 
      - completed
  
jobs:
  black:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          make format
